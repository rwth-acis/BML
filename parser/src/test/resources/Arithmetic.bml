Bot(host="localhost", port=8080) {

    @Routine(rate="1h")
    incompatibleTypes(data) {
        a = 1
        b = "asdf"
        c = a + b
    }

    @Routine(rate="1h")
    integralArrayAccess(data) {
        a = 1.0
        c = ["asdf", "asdf2"]
    }
}
