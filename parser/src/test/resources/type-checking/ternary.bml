Bot(host="localhost", port=8080) {

    @Routine(rate="1h")
    nestedTernaryWithWrongType(data) {
        a = 1 == 0 ? 1 : "a" == "b" ? 0 : "c"
    }

    @Routine(rate="1h")
    nestedTernaryWithWrongType(data) {
        a = "a" == "b" ? 1 > 0 ? "a" : "b" : 1
    }

    @Routine(rate="1h")
    booleanCondition(data) {
        a = "a" + "b" ? 1 : 0
    }

    @Routine(rate="1h")
    argumentTypesAreDifferent(data) {
        a = "a" > "b" ? 1 : "a"
    }
}