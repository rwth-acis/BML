Bot(host="localhost", port=8080) {

    @Routine(rate="1h")
    wrongTypeInOperationAssignment(data) {
        a = 1
        a += "test"
    }

    @Routine(rate="1h")
    wrongTypeInOperationAssignment(data) {
        a = 1
        a += [1, 2, 3]
    }

    @Routine(rate="1h")
    reassignment(data) { // ALLOWED
        a = 1
        a = [1, 2, 3]
    }
}