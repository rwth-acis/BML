Bot(host="localhost", port=8080) {

    @Routine(rate="1h")
    wrongTypeOnLeftOfRelation(data) {
        a = 0
        b = true
        c = b > a
    }

    @Routine(rate="1h")
    wrongTypeOnRightOfRelation(data) {
        a = 1
        b = false
        c = a > b
    }

    @Routine(rate="1h")
    wrongTypeOnLeftOfRelation(data) {
        a = "abcd"
        b = "abc"
        c = a > b
    }
}
